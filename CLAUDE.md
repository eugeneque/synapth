# Synapth — заметки для агента

- Отвечать по-русски; код и комментарии в коде — по-английски.
- Серверные модули только в `cortex/`, клиентские — в `axon/`; `lib/` — изоморфные утилиты. Route handlers в `app/api` не содержат бизнес-логики.
- Деньги — целые микродоллары (`types/economy.ts`), никакого float в леджере.
- `Verified` никогда не присваивается сканером — только через `reviewed: true`.
- БД: `DATABASE_URL` или `NETLIFY_DATABASE_URL` (Neon через Netlify DB) — резолвится в `cortex/db.ts` (`databaseUrl`). Схема на Neon синхронизируется `scripts/db-sync.mjs` (в сборке Netlify и `npm run db:sync`): разовые SQL-миграции данных + `prisma db push` без `--accept-data-loss`. Деструктивное изменение схемы → оформляй SQL-шагом в `PRE_PUSH_SQL`, а не флагом.
- Без `DATABASE_URL` всё работает на in-memory сторе (`cortex/repository.ts`, `cortex/billing.ts`); тесты (`npm test`) рассчитаны на этот режим.
- Проверка: `npm run typecheck && npm test && npm run build`.
- Каталог живёт в `data/catalog.json` (gitignored), обход — `npm run crawl`, бейджи — `npm run rescan`. Тесты пишут в `.test-catalog.json`, реальный каталог не трогают.
- Синглтоны в `globalThis` версионируются ключом (`__synapthRepo_v3`): при смене интерфейса репозитория поднимай версию, иначе HMR оставит старый экземпляр.
- Токен GitHub для краулера/импорта: `GITHUB_TOKEN` или `gh auth token` (`cortex/github-token.ts`); в `.env` его не записывать.
- Локализация: все строки UI — через `lib/i18n` (ключи в `messages/en.ts`, остальные локали типизированы по нему). Сервер — `getI18n()` из `cortex/locale.ts`, клиент — `useI18n()` из `axon/i18n.tsx`. Локаль — cookie `synapth_locale`, без префикса в URL. Инлайн-разметка в строках: `**strong**`, `*em*`, `` `code` `` → `rich()`.
- Маршруты: страницы сайта лежат в `app/(site)/` (общий хедер/футер в `app/(site)/layout.tsx`), auth-gate — в `app/(auth)/` со своим минимальным баром; корневой `app/layout.tsx` даёт только `<html>`, шрифты, `PixelField` и `I18nProvider`. `app/not-found.tsx` — глобальный 404 (оборачивает `(site)/not-found.tsx` в хедер/футер).
- Хедер: ссылки «Консоль» в `NAV` нет, кнопки «Опубликовать» тоже; аватар ведёт на `/u/<handle>`, сердце — на `/favorites` (свои скиллсеты, избранные скиллсеты, отслеживаемые записи; `/dashboard/skillsets` — редирект).
- Панель управления (`/dashboard/*`): `app/(site)/dashboard/layout.tsx` рисует рейл `ConsoleNav` (Настройки → Публикация и ключи → Уведомления → Администрирование → Админ-панель → Публичный профиль). `/dashboard` редиректит на `/dashboard/settings`; публикация с GitHub и API-ключи — одна страница `/dashboard/developer` (`#publish`, `#keys`). «Администрирование» — route group `dashboard/(staff)/` с общим layout (право `catalog.moderate`) и табами `StaffTabs`: `/dashboard/moderation` и `/dashboard/verification` (таб и страницы — `users.verify`; старые `/dashboard/admin/verification/*` редиректят). Настройки аккаунта — `/dashboard/settings` (`cortex/account.ts`, server action в `settings/actions.ts`, после сохранения `updateSession()` обновляет JWT). Предпочтение клиента установки зеркалится в cookie `synapth_target`, его читает `defaultTarget()` из `axon/install.ts`.
- Профили: публичная страница пользователя — `/u/[handle]`; `/authors/[owner]` остаётся для GitHub-владельцев из краулера и редиректит на `/u/…`, если хендл принадлежит зарегистрированному аккаунту. Аватар (`User.image`) и обложка (`coverImage`) хранятся как `data:image/*;base64` (клиент сжимает в `axon/image.ts`, лимиты — `types/profile.ts`); в JWT картинку не класть — хедер берёт её из `getProfile()`. Список профессий (`occupation`) — `OCCUPATIONS` в `types/profile.ts`, подписи — ключи `occupation.<id>`.
- Схема Prisma: у `User` добавлены `bio`, `organization`, `website`, `location`, `occupation`, `coverImage`, `defaultTarget` — при реальной БД нужен `npm run db:push`.
- `memoryUsers` (in-memory юзеры) тоже живут в `globalThis` (`__synapthUsers_v2`) — по той же причине, что и репозиторий.
- Соцслой (`cortex/social.ts`): «лайк» называется **импульс** (`Impulse`, один на пару from→to, тумблер), посты (`Post`), комментарии (`Comment`, полиморфные: `targetKind` post|skill — навыки/MCP/инструменты это один `Skill`), отслеживание (`SkillWatch`). Мутации идут через server actions в `app/(site)/social-actions.ts`; после событий вызывается `evaluateBadges()`.
- Уведомления (`cortex/notifications.ts`): чистый inbox, виды в `NOTIFICATION_KINDS` (`types/social.ts`), у каждого — канал для табов (`NOTIFICATION_CHANNEL`). Репозиторий при смене версии навыка зовёт `notifySkillUpdated()` (watchers, кроме автора). Клиент: `axon/notifications.tsx` поллит `GET /api/v1/notifications?after=` каждые 20 с, новые строки → тост (`axon/toast.tsx`, `useToast()`) + чайм `public/sounds/notification.mp3` (mute в localStorage `synapth_notif_muted`). Шторка — `components/notification-center.tsx`, страница — `/dashboard/notifications`. Тексты карточек/тостов — `axon/notification-text.ts` (у actor-фраз `{name}` всегда первым, `verb` = фраза без имени).
- Бейджи (`types/badges.ts` + `cortex/badges.ts`): каталог в коде (`BADGES`, критерии `BADGE_CRITERIA`), в сторе только выдачи (`UserBadge`). `evaluateBadges()` идемпотентен; ручные (`award: "manual"`) — только `grantBadge()` админом. Подписи — ключи `badge.<id>.title|body`.
- Новые Prisma-модели: `Impulse`, `Post`, `Comment`, `SkillWatch`, `Notification`, `UserBadge` — `npm run db:push` при реальной БД. In-memory демо-строки соцслоя — в конце `cortex/seed.ts`.
- Регистрация — `cortex/registration.ts`: пароль (`registerWithPassword`, route `/api/auth/register` — тонкий) и первый OAuth-вход (`createOAuthUser`, подключён как `adapter.createUser` в `cortex/auth.ts`). Оба создают юзера + кошелёк + welcome-кредит одной вставкой. Хендлы — `handleSchema`/`isReservedHandle` (служебные слова и префикс `gh-` краулера). Ошибки с `code` → `auth.err.<code>` на клиенте. На serverless без БД регистрация отдаёт 503 (иначе аккаунт жил бы в памяти одной лямбды).
- Роли: `user` < `moderator` < `admin`; права — `PERMISSIONS`/`can()` в `types/auth.ts` (admin = все, включая будущие). Проверки — `cortex/roles.ts` (`requirePermission`, `hasPermission`) по **роли из стора**, не из JWT; для сессии — `requireSessionPermission`, для API-ключей — `requireCallerPermission`. Верификация каталога — `cortex/moderation.ts` (`setVerification`: сканер с `reviewed: true`, аудит в `SecurityScan.reviewedBy`). Смена ролей — только админ, не себе, не последнему админу; первый админ — `npm run role -- <email|handle> admin`.
- Безопасность (не откатывать):
  - Любой пользовательский URL — только через `lib/url-safety.ts` (`httpUrlSchema` в zod-схемах, `safeExternalHref`/`safeImageSrc` при рендере, `safeCallbackPath` для редиректов). Голый `z.string().url()` пропускает `javascript:` и `data:`.
  - Серверный `fetch()` по адресу из пользовательских данных — только `safeFetch()`/`assertPublicUrl()` из `cortex/ssrf.ts` (DNS-резолв, приватные диапазоны, редиректы, лимит тела). Локальная отладка — `SYNAPTH_ALLOW_PRIVATE_FETCH=1` (в проде игнорируется).
  - Каждый мутирующий и дорогой эндпоинт + server action проходит через `cortex/rate-limit.ts` (`enforceRequestLimit` / `enforceRateLimit`); бюджеты — в `RATE_LIMITS`.
  - Краулер (`/api/v1/crawl`, панель в админ-панели) — только право `crawler.run` (admin, `requireCallerPermission`); в in-memory демо админ — `usr_demo`.
  - `POST /api/cron/crawl` закрыт без `SYNAPTH_CRON_SECRET` (bearer, timing-safe) — не открывать его без секрета.
  - `upsertMany()` не перезаписывает навык с чужим `authorId` (считает в `skipped`) — иначе импорт похожего репозитория угоняет чужой слаг.
  - Заголовки безопасности и CSP — в `next.config.ts`; CORS `*` только для публичного чтения каталога (`PUBLIC_API_PATHS`), приватные `/api/v1/*` — same-origin.
  - Ответы апстрима в `/skills/:id/execute` наружу не проксируются (только свои сообщения) — тело апстрима это канал SSRF-утечки.
- Публичность ролей: на профиле, в ховеркарте и `GET /api/v1/users/[handle]/card` роль идёт через `publicRole()` (`types/auth.ts`) — admin показывается как `user`. Бейдж `platform-admin` удалён (строки чистит `PRE_PUSH_SQL` в `db-sync`).
- Developer — не роль, а флаг `User.developer` (прав не даёт): ставит/снимает только admin (`users.manageRoles`, можно себе) через `DeveloperToggle` в `/dashboard/admin/users` → `setDeveloper()` в `cortex/developers.ts` (не в `roles.ts`: его импортирует `cortex/auth.ts`, бейджи тянут `node:fs`). Флаг даёт уникальное достижение `platform-developer` (тир `astra`, девиз «Per Aspera Ad Astra»): у `unique`-бейджей `evaluateBadges()` также снимает выдачу, когда критерий перестал выполняться, `grantBadge()` их не выдаёт. Фильтр админки `?role=developer`.
- Админ-панель: `/dashboard/admin` (обзор, `/users`, `/crawler`), право `admin.access` (только admin), вход — отдельный пункт рейла. `/dashboard/users` — редирект.
- Автокраулер (`cortex/crawl-jobs.ts`): все запуски (ручной/по расписанию) идут через него и пишутся в `CrawlRun` (in-memory — `__synapthCrawlRuns_v1`). Каждые 2 ч по сетке UTC: на Netlify — scheduled function `netlify/functions/crawl-cron.mjs` → `POST /api/cron/crawl` (нужен `SYNAPTH_CRON_SECRET` в env сайта, проход ограничен ~20 с и пропускает репо, уже есть в каталоге); под `next start` — таймер из `instrumentation.ts`. `SYNAPTH_AUTO_CRAWL=0` выключает, `=1` включает в dev.
- Модерация по заявкам (`cortex/moderation.ts`, `types/moderation.ts`): кнопка «Отправить на модерацию» на странице записи → `ModerationRequest` (одна открытая на запись) → уведомление `moderation.requested` всем moderator/admin → разбор на `/dashboard/moderation/[id]` (манифест, README, перезапуск сканера, вердикт) → `moderation.decided` автору заявки и автору записи. Одобрение идёт через `setVerification` (сканер может отказать — заявка остаётся открытой), отклонение требует комментарий.
- Новые Prisma-модели: `ModerationRequest`, `CrawlRun` — `npm run db:push` при реальной БД (на Netlify это делает `db-sync`).
- Скиллсеты (`cortex/skillsets.ts`, `types/skillset.ts`): наборы записей каталога, создаются только на платформе (`/skillsets/new`, `/skillsets/[slug]/edit` — только автор), краулер их не трогает. Мутации — server actions в `app/(site)/skillset-actions.ts`. Каждое добавление/удаление записи и правка полей пишется в `SkillsetChange` (история читается по порядку вставки: `seq` в БД, порядок массива в памяти). `verified` ставит только `catalog.verify` (`setSkillsetVerification`), любое изменение состава снимает его автоматически (строка `unverified` с `auto: true`) и шлёт `skillset.updated` добавившим в избранное. Слаги транслитерируются из кириллицы, `new`/`edit`/`images` зарезервированы.
- Картинки в описании скиллсета — отдельные строки `SkillsetImage` (base64), отдаются `GET /api/v1/skillsets/images/[id]`; в markdown только путь `SKILLSET_IMAGE_PATH<id>`, `Markdown` пропускает его через `localImagePrefixes`. Аватар — `data:image/*` как у профиля.
- Установка скиллсета одной командой — `skillsetInstall()` в `axon/install.ts`: `curl -fsSL …/api/v1/skillsets/<slug>/install?target=claude-code|cursor | sh`. Все данные манифестов в скрипте — только через `shellQuote()`, heredoc-терминатор уникален (`heredocTag`); Sandbox-записи и HTTP-инструменты в скрипт не попадают.
- Новые Prisma-модели: `Skillset`, `SkillsetItem`, `SkillsetChange`, `SkillsetFavorite`, `SkillsetImage` — `npm run db:push` при реальной БД (на Netlify — `db-sync`). In-memory демо-скиллсеты — в конце `cortex/seed.ts`.
- Ховеркарт пользователя: `UserHoverCard` (`components/user-hover-card.tsx`) — долгое наведение (550 мс, только мышь) на `Avatar link` или аватар в хедере → карточка из `GET /api/v1/users/[handle]/card` (`cortex/user-card.ts`, только публичные поля профиля + `viewer`), кэш на клиенте по хендлу. Шкала достижений — один сегмент на бейдж каталога.
- Профиль: шапка — аватар, заголовок (профессия · организация), локация, счётчики (друзья/посты/навыки), основное действие (`FriendButton` или «Редактировать» у владельца) + меню `ProfileMoreMenu`; справа — ссылки (организация, сайт, GitHub). Ниже — импульс + статы и вкладки `ProfileDetails` «О разработчике / Направления / Достижения» (хэш `#about|#focus|#badges`). Друзья — боковая карточка `FriendsCard` (`#friends`) справа от навыков/постов: строки с круглой `FriendButton size="icon"` (отношение зрителя к каждому — `friendStates()`), «Показать ещё» после 6; владельцу там же входящие заявки сверху и сноска со своими заявками без ответа.
- Реакции на посты: `PostReaction` (post, user, emoji — тумблер; от одного юзера можно несколько разных эмодзи), набор — `POST_REACTIONS` в `types/social.ts`, стор — `toggleReaction`/`reactionCounts` в `cortex/social.ts` (без уведомлений), экшен `reactToPost`, UI — `components/post-reactions.tsx` (оптимистично). `Post.reactions` считается под зрителя: `listPosts(authorId, { viewerId })`. In-memory — `__synapthSocial_v3`, демо-строки — `seedReactions`. Новая модель — `npm run db:push` при реальной БД (на Netlify — `db-sync`). `ImpulseButton` принимает `viewer: ImpulseViewer` (`types/social.ts`), `size="sm"` — чип для ховеркарта.
- Посты: тело — не Markdown, а мини-грамматика `lib/post-body.ts` (```lang-фенсы, `inline code`, `@handle`). Сервер (`cortex/post-content.ts`) парсит в `Post.content: PostBlock[]`: код токенизирован lowlight (hast → токены, без HTML-строк и `dangerouslySetInnerHTML`), язык без метки угадывает `detectLanguage()` из `lib/code-lang.ts` (сигнатуры; фолбэк — relevance highlight.js), упоминания — только для существующих хендлов (`getAuthorRefsByHandles`). lowlight не импортировать в клиент. Упомянутым — уведомление `post.mention` (вместо `post.new`, лимит `POST_MAX_MENTION_NOTIFY`, код не считается). Цвета токенов — `.code-tokens .hljs-*` в `globals.css`.
- Фото постов (карусель): модель `PostImage` (base64, `postId` null = черновик). Композер (`components/post-composer.tsx`) сжимает (`prepareImage`, fit contain, `POST_IMAGE`) и грузит по одной через `uploadPostPhoto` (лимит `postImage`), `publishPost(body, handle, imageIds)` прикрепляет в порядке ленты (только свои черновики, ≤ `POST_MAX_IMAGES`). Черновики старше суток чистит следующая загрузка. Отдаёт `GET /api/v1/posts/images/[id]` — только прикреплённые. Рендер — `PostCarousel` (scroll-snap). In-memory стор соцслоя — `__synapthSocial_v3`. Новая модель — `npm run db:push` при реальной БД (на Netlify — `db-sync`).
- Анимации: кейфреймы — в `tailwind.config.ts` (`rise`, `pop-in`, `impulse-*`, `count-*`, `bell-ring`…), утилиты `.stagger`, `.lift`, `.skeleton` — в `globals.css`; `app/(site)/template.tsx` даёт вход страницы. Вход — с `backwards` и `transform: none` в конце (иначе `position: fixed` внутри поедет). `prefers-reduced-motion` глушит всё глобально.
- Верификация аккаунта (галочка): `types/verification.ts` (пороги `VERIFICATION_RULES`: 365 дней, 500 импульсов + телефон E.164; этапы `submitted → scan → review → decision`, лог событий в `events`) + `cortex/verification.ts`. Сканер (этап `scan`) прогоняет все навыки заявителя сразу при подаче: любая находка → заявка `rejected` (`scan_failed`); при одобрении скан повторяется. Галочку ставит только право `users.verify` (admin): по заявке (`/dashboard/verification/[id]`: взять в работу → одобрить/отклонить с причиной) или вручную (`CheckMarkToggle` в `/dashboard/admin/users`), не себе. Хранится в `User.verifiedAt|verifiedById|verifiedVia` (in-memory — `__synapthVerified_v1` в `cortex/account.ts`), попадает в `AccountProfile.verified` и `AuthorRef.verified` → `VerifiedMark`. Телефон полностью видят только админы, заявителю — маска. Настройки поллят `GET /api/v1/account/verification` (8 с + на каждое уведомление). Уведомления `verification.requested` (админам) / `verification.updated` (заявителю).
- Новая Prisma-модель `VerificationRequest` и поля `User.verified*` — `npm run db:push` при реальной БД (на Netlify — `db-sync`).
- Tailwind: после правки `tailwind.config.ts` dev-сервер может отдавать старый CSS из кэша — `touch app/globals.css`.
- Друзья (`cortex/friends.ts`): дружба = взаимный `Follow` (from→to). Односторонний follow — неотвеченная заявка: отправитель уже получает посты получателя (`post.new` из `createPost` всем `followerIds`), обратно — нет. `toggleFollow` удаляет только свою строку (расфрендить = заявка другой стороны снова «входящая»). Состояние для зрителя — `FriendState` (`types/social.ts`), кнопка — `FriendButton`, экшен — `toggleFriend` в `social-actions.ts`. Уведомления `friend.request` / `friend.accepted` / `post.new` (канал social). Список друзей публичный (вкладка `#friends` профиля), заявки видит только владелец. In-memory — `__synapthFollows_v1`, демо-строки — `seedFollows`.
- Глобальный поиск: блок в хедере (`components/search-trigger.tsx`, ⌘K) → `GET /api/v1/search/global` (`cortex/global-search.ts`): 10 смешанных строк — люди (совпадение по хендлу/имени первыми, `@` — только люди), навыки/MCP/плагины, скиллсеты; последняя строка «Открыть поиск» → `/search`. Отдельных пунктов «Каталог»/«Люди» в `NAV` нет.
- Страница `/search` — дом каталога и людей: вкладки `tab=all|people|skills|skillsets`, общий `q` (`SearchPageInput`), фильтр источника `source=github|synapth` (`skillSource()` в `types/skill.ts`: github = `origin === "github"`, остальное — On Synapth; на «Всё» GitHub прячет людей и скиллсеты). `/explore`, `/people`, `/skillsets` — редиректы с сохранением фильтров. На нетронутых вкладках навыков и скиллсетов — `FeaturedSlider` (5 с, пауза по hover/focus, таймер на JS, не `animationend`: reduced-motion обнуляет CSS-длительности). Витрина на `/search` рендерится с `key` по `q|source` и `hideSearch`. Серверный JSX, передаваемый в пропе клиентскому компоненту, выносить в отдельный компонент (иначе React 19 ругается на key).
- Новая Prisma-модель `Follow` — `npm run db:push` при реальной БД (на Netlify — `db-sync`).
- Выход — только `SignOutButton` (`components/sign-out-button.tsx`): `signOut({ redirect: false })` из `next-auth/react` + жёсткий переход на `/signin`. Не возвращать server action с `signOut()`. `middleware.ts` (Auth.js перевыставляет cookie сессии в каждом ответе) матчит только `/dashboard/*` и пропускает запросы с `next-action`/`next-router-prefetch` — иначе параллельный запрос воскрешает удалённую cookie.
- Системный аккаунт комиссий — `usr_platform` с хендлом `platform` (создаёт `db-sync`, без почты и пароля, войти нельзя). Хендл `synapth` свободен для настоящего официального аккаунта; оба в `RESERVED_HANDLES`, поэтому ставятся только через БД.
- Подтверждение почты (`cortex/email-verification.ts`, почта — `cortex/mailer.ts`): парольный аккаунт создаётся с `emailVerified = null`, регистрация сразу шлёт 6-значный код (хранится только SHA-256 в `EmailCode`, 15 мин, 5 попыток, повтор не чаще раза в минуту). Credentials-вход до подтверждения бросает `CredentialsSignin` с `code = "email_unverified"` → `AuthGate` показывает `EmailCodeStep`. Роуты — `/api/auth/verify-email` и `/verify-email/resend` (ответы не раскрывают, есть ли аккаунт). OAuth не гейтится. Неподтверждённый адрес перехватывается новой регистрацией (защита от сквоттинга чужой почты). Транспорт — Resend (`RESEND_API_KEY`, `EMAIL_FROM`); без ключа код печатается в консоль сервера, а в проде подтверждение выключается (`SYNAPTH_EMAIL_VERIFICATION=0|1` — ручной override). Модуль импортируется из `cortex/auth.ts` → в middleware (edge): только Web Crypto, никакого `node:crypto`. Старые парольные аккаунты помечает подтверждёнными `PRE_PUSH_SQL` в `db-sync` (по дате 2026-09-25). Новая Prisma-модель `EmailCode` — `npm run db:push` при реальной БД.
